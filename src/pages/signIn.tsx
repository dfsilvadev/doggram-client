import { FormsTemplate } from "@/templates";

import { SignInForm } from "@/components";

const SignIn = () => {
  return (
    <FormsTemplate>
      <SignInForm />
    </FormsTemplate>
  );
};

export default SignIn;
